
logging:
  level:
    sql: debug

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
  jackson:
    serialization:
      INDENT_OUTPUT: true


